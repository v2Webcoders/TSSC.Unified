@model TSSC.Unified.ViewModel.MyReimbursementVM

@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    Layout = "_Layout";
    int serial = 1;
}

<div class="nk-block-head nk-block-head-sm">
    <div class="nk-block-between">

        <div class="nk-block-head-content">
            <h3 class="nk-block-title page-title">My Reimbursement List</h3>
        </div>

        <div class="nk-block-head-content">
            <a asp-action="Create"
               asp-controller="Reimbursement"
               class="btn btn-primary">
                <em class="icon ni ni-plus"></em>
                Apply Reimbursement
            </a>
        </div>

    </div>
</div>

<div class="card card-preview">
    <div class="card-inner">

        <table class="datatable-init-export table table-tranx table_custom table-bordered">

            <thead>
                <tr>
                    <th>S.No.</th>
                    <th>Request Type</th>
                    <th>Expense Category</th>
                    <th>Expense Amount</th>
                    <th>Expense Date</th>
                    <th>Vendor Name</th>
                    <th>Payment Mode</th>
                    <th>Supporting Document</th>
                    <th>Invoice / Bill</th>
                    <th>Status</th>
                    <th>Action</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model.ReimbursementList)
                {
                    <tr>
                        <td>@serial</td>
                        <td>@item.RequestType</td>
                        <td>@item.ExpenseCategory</td>
                        <td>₹ @item.ExpenseAmount.ToString("N2")</td>
                        <td>@item.ExpenseDate.ToString("dd-MMM-yyyy")</td>
                        <td>@item.VendorName</td>
                        <td>@item.PaymentMode</td>
                        <td>
                            @if (!string.IsNullOrEmpty(item.SupportingDocument))
                            {
                                <a href="@Url.Content(item.SupportingDocument)"
                                   target="_blank"
                                   class="btn btn-sm btn-info">
                                    <em class="icon ni ni-file"></em>
                                    View File
                                </a>
                            }
                            else
                            {
                                <span>-</span>
                            }
                        </td>

                        <td>
                            @if (!string.IsNullOrEmpty(item.InvoiceBill))
                            {
                                <a href="@Url.Content(item.InvoiceBill)"
                                   target="_blank"
                                   class="btn btn-sm btn-info">
                                    <em class="icon ni ni-file"></em>
                                    View Bill
                                </a>
                            }
                            else
                            {
                                <span>-</span>
                            }
                        </td>
                        <td>@item.Status</td>
                        <td>
                            <div class="btn-group">
                                <a asp-action="Create"
                                   asp-controller="Reimbursement"
                                   asp-route-id="@item.ReimbursementId"
                                   class="btn btn-sm btn-outline-warning">
                                    <em class="icon ni ni-edit"></em>
                                </a>
                                <a asp-action="Delete"
                                   asp-controller="Reimbursement"
                                   asp-route-id="@item.ReimbursementId"
                                   class="btn btn-sm btn-outline-danger">
                                    <em class="icon ni ni-trash"></em>
                                </a>
                            </div>
                        </td>
                    </tr>
                    serial++;
                }
            </tbody>

        </table>

    </div>
</div>