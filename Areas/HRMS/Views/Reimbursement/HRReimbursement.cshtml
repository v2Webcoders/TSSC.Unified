@model IEnumerable<TSSC.Unified.ViewModel.ReimbursementVM>

@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    Layout = "_Layout";
    int serial = 1;
}

<div class="nk-block-head nk-block-head-sm">
    <div class="nk-block-between">

        <div class="nk-block-head-content">
            <h3 class="nk-block-title page-title">
                Reimbursement List
            </h3>
        </div>

    </div>
</div>

<div class="card card-preview">
    <div class="card-inner">

        <table class="datatable-init-export table table-tranx table_custom table-bordered">

            <thead>
                <tr>
                    <th>S.No.</th>
                    <th>Employee</th>
                    <th>Expense Category</th>
                    <th>Expense Amount</th>
                    <th>Expense Date</th>
                    <th>Payment Mode</th>
                    <th>Supporting Document</th>
                    <th>Invoice / Bill</th>
                    <th>Status</th>
                    <th>Action</th>
                </tr>
            </thead>

            <tbody>

                @foreach (var item in Model)
                {
                    <tr>

                        <td>
                            @serial
                        </td>

                        <td>
                            @item.EmployeeId
                        </td>

                        <td>
                            @item.ExpenseCategory
                        </td>

                        <td>
                            ₹ @item.ExpenseAmount.ToString("N2")
                        </td>

                        <td>
                            @item.ExpenseDate.ToString("dd-MMM-yyyy")
                        </td>

                        <td>
                            @item.PaymentMode
                        </td>

                        <td>
                            @if (!string.IsNullOrEmpty(item.SupportingDocument))
                            {
                                <a href="@Url.Content(item.SupportingDocument)"
                                   target="_blank"
                                   class="btn btn-sm btn-info">

                                    <em class="icon ni ni-file"></em>
                                    View File

                                </a>
                            }
                            else
                            {
                                <span>-</span>
                            }
                        </td>

                        <td>
                            @if (!string.IsNullOrEmpty(item.InvoiceBill))
                            {
                                <a href="@Url.Content(item.InvoiceBill)"
                                   target="_blank"
                                   class="btn btn-sm btn-info">

                                    <em class="icon ni ni-file"></em>
                                    View Bill

                                </a>
                            }
                            else
                            {
                                <span>-</span>
                            }
                        </td>

                        <td>
                            @item.HRStatus
                           @*  @if (item.FinanceStatus == "Finance Approved"
                                                    && item.HRStatus == "Pending")
                            {
                                <span class="badge bg-warning">
                                    HR Pending
                                </span>
                            }
                            else if (item.HRStatus == "HR Approved")
                            {
                                <span class="badge bg-success">
                                    Approved by HR
                                </span>
                            }
                            else if (item.HRStatus == "HR Rejected")
                            {
                                <span class="badge bg-danger">
                                    Rejected by HR
                                </span>
                            } *@

                        </td>

                        <td>

                            @if (item.Status == "Manager Approved")
                            {
                                <span class="badge bg-success">
                                    Approved by Manager
                                </span>
                            }

                            @if (item.FinanceStatus == "Finance Approved")
                            {
                                <span class="badge bg-success">
                                    Approved by Finance
                                </span>
                            }

                            @if (item.HRStatus == "HR Approved")
                            {
                                <span class="badge bg-success">
                                    Approved by HR
                                </span>
                            }

                            @if (item.HRStatus == "HR Rejected")
                            {
                                <span class="badge bg-danger">
                                    Rejected by HR
                                </span>
                            }

                            @if (item.HRStatus == "Pending")
                            {
                                <form method="post">

                                    <input type="hidden"
                                           name="id"
                                           value="@item.ReimbursementId" />

                                    <input type="text"
                                           name="remarks"
                                           class="form-control form-control-sm mb-1"
                                           placeholder="HR Remarks" />

                                    <button type="submit"
                                            formaction="@Url.Action("HRApprove", "Reimbursement")"
                                            class="btn btn-sm btn-success">

                                        <em class="icon ni ni-check"></em>
                                        Approve

                                    </button>

                                    <button type="submit"
                                            formaction="@Url.Action("HRReject", "Reimbursement")"
                                            class="btn btn-sm btn-danger">

                                        <em class="icon ni ni-cross"></em>
                                        Reject

                                    </button>

                                </form>
                            }

                        </td>

                    </tr>

                    serial++;
                }

            </tbody>

        </table>

    </div>
</div>