@model List<TSSC.Unified.ViewModel.ReimbursementsVM>

@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    Layout = "_Layout";
    ViewData["Title"] = "Approved List";

    int serial = 1;
}

<div class="nk-block-head nk-block-head-sm">

    <div class="nk-block-between">

        <div class="nk-block-head-content">

            <h3 class="nk-block-title page-title">
                Approved list
            </h3>

        </div>

    </div>

</div>


<div class="nk-content-inner">

    <div class="nk-content-body">

        <div class="card card-bordered">

            <div class="card-inner">

                <table class="datatable-init-export table table-tranx table_custom table-bordered dataTable no-footer dtr-inline collapsed">

                    <thead>

                        <tr>

                            <th>S.No.</th>

                            <th>Request Type</th>

                            <th>Expense Category</th>

                            <th>Amount</th>

                            <th>Expense Date</th>

                            <th>Vendor Name</th>

                            <th>Purpose</th>

                            <th>Supporting Document</th>

                            <th>Invoice / Bill</th>

                            <th>My Remark</th>

                            <th>Approved On</th>

                        </tr>

                    </thead>


                    <tbody>

                        @if (Model != null && Model.Any())
                        {
                            foreach (var item in Model)
                            {
                                <tr>

                                    <td>
                                        @serial
                                    </td>


                                    <td>
                                        @item.RequestType
                                    </td>


                                    <td>
                                        @item.ExpenseCategory
                                    </td>


                                    <td>
                                        ₹ @item.ExpenseAmount.ToString("N2")
                                    </td>


                                    <td>
                                        @item.ExpenseDate.ToString("dd-MMM-yyyy")
                                    </td>


                                    <td>
                                        @(string.IsNullOrEmpty(item.VendorName)
                                                                        ? "-"
                                                                        : item.VendorName)
                                                                                      </td>


                                    <td>
                                        @(string.IsNullOrEmpty(item.Purpose)
                                                                        ? "-"
                                                                        : item.Purpose)
                            </td>


                                    <!-- Supporting Document -->

                                    <td>

                                @if (!string.IsNullOrEmpty(item.SupportingDocument))
                                        {
                                            <a href="@Url.Content(item.SupportingDocument)"
                                               target="_blank"
                                               class="btn btn-sm btn-outline-info">

                                                <em class="icon ni ni-file-text"></em>

                                                View File

                                            </a>
                                        }
                                        else
                                        {
                                            <span>-</span>
                                        }

                                    </td>


                                    <!-- Invoice / Bill -->

                                    <td>

                                        @if (!string.IsNullOrEmpty(item.InvoiceBill))
                                        {
                                            <a href="@Url.Content(item.InvoiceBill)"
                                               target="_blank"
                                               class="btn btn-sm btn-outline-info">

                                                <em class="icon ni ni-file-text"></em>

                                                View Bill

                                            </a>
                                        }
                                        else
                                        {
                                            <span>-</span>
                                        }

                            </td>


                            <!-- My Remark -->

                            <td>

                                @(string.IsNullOrEmpty(item.Remarks)
                                                                ? "-"
                                                                : item.Remarks)

                            </td>


                            <!-- Approved On -->

                            <td>

                                @(item.UpdatedOn.HasValue
                                                                ? item.UpdatedOn.Value.ToString("dd-MMM-yyyy")
                                                                : "-")

                            </td>

                                                </tr>

                                                serial++;
                            }
                        }
                        else
                        {
                            <tr>

                                <td colspan="11"
                                    class="text-center">

                                    No approved list found.

                                </td>

                            </tr>
                        }

                    </tbody>

                </table>

            </div>

        </div>

    </div>

</div>